<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>Mot de passe oublié</title>
    </head>
    <body>
        <h3>Bonjour {{ user.firstname }}, </h3>
        <p>
            Vous pouvez réinitialiser votre mot de passe en cliquant sur
            {% if app.environment == 'dev' %}
                <a href="http://127.0.0.1:8000/#/users/{{ user.resetPasswordToken }}/reset/password">ce lien</a>
            {% else %}
                <a href="https://gestion-sci/#/users/{{ user.resetPasswordToken }}/reset/password">ce lien</a>
            {% endif %}
        </p>
        <p>* <em>Ce lien est valable 15 min</em></p>
    </body>
</html>